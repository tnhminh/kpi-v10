# Project Status

Last updated: 2026-08-25

## Overall
**Phase:** Productionization
**Production readiness:** NOT READY
**Current task:** T16 — Remote container image/runtime evidence (T15 remote CI PASS; T14 externally blocked)

## Verification baseline
- `npm run lint`: PASS after T10-G observability hardening (2026-08-25)
- `npm run typecheck`: PASS after T10-G observability hardening (2026-08-25)
- `npm run test`: PASS, 31 files / 146 tests (2026-08-25)
- `npm run build`: PASS, Next.js 16.3.2 standalone production build including Administration onboarding, `/api/metrics`, request Proxy correlation, and server instrumentation (2026-08-25)
- `npm ci`: PASS from lockfile; 443 packages installed and audit returned 0 vulnerabilities (2026-08-25).
- `npm run proof:release-local`: PASS all 12 local release steps after fixing shell/env inheritance in the cross-platform orchestrator (2026-08-25).
- Local development preview: HTTP 200 on port 3712 with `.env.local` loaded. Real browser regression now uses Playwright against installed system Chrome and PASSes login, high-value Team mutation, reload persistence, critical navigation and zero page errors.
- Local PostgreSQL integration (2026-08-25): migrations `0001`–`0014` applied to `kpi_v10_dev`; `/api/ready` 200; credentialed auth/organization APIs 200; live T06 batch persisted 14/14 eligible member evaluations and 55 evaluated criteria; live T07 flow reached Leader → Head → Finalize → LOCKED with a checksum-protected snapshot and post-lock mutation probes blocked by PostgreSQL; T08 Jira snapshot integrity and T09 append-only audit/history authority are installed and live-proven locally.
- Local persistent demo seed (2026-08-25): `npm run db:seed:local` provisions 6 teams, 14 members, Backend Engineering KPI v1 PUBLISHED + v2 DRAFT with 5 criteria, metric/scoring configuration, rank scheme, and 2026-09 period assignments. Authenticated API verification returned Teams=6, Members=14, Templates=1 and Builder v2 criteria=5.
- Local Jira demo evidence (2026-08-25): migration `0011` is applied and `npm run db:seed:jira-demo` persistently provisions one Jira demo workspace, 14 explicit member↔Jira account mappings, 87 Jira-shaped issues/current facts, 84 mapped + 3 intentionally unmapped issues, and an idempotent successful demo sync-run. Coverage fixtures include 5 missing completion-date observations, 3 missing story-point observations and 4 missing resolution-minute observations. See `docs/JIRA_DEMO_DATA.md`.
- T08-B live Jira evaluation proof (2026-08-25): `npm run db:evaluate:jira-demo` used a short-lived in-process verifier identity, authenticated with HTTP 200, evaluated 14/14 eligible members twice, persisted 14 evaluations and 84 immutable `jira_fact_snapshots`, then deliberately mutated one Jira current fact. The second Jira evaluation retained the same snapshot digest and the same persisted evaluation scores, proving reruns use frozen facts. Four members correctly surfaced CRITICAL data-quality state from seeded incomplete Jira observations; the verifier identity and temporary fact mutation were cleaned up.

## Completed
- Contest prototype application shell and major workflow screens.
- Configurable KPI Builder UI with live score explanation trace.
- Demo views for evaluation, review, calibration, Jira snapshots, historical analytics, rank scheme, and audit log.

## Completed productionization tasks
- T00 Governance and handoff baseline: docs created; lint PASS; production build PASS; documentation consistency reviewed.
- T01 Production foundation: env contract, security headers, health/readiness routes, structured logging/request IDs, request validation helpers, test/typecheck scripts; verification PASS.
- T02 PostgreSQL domain model: Drizzle runtime schema + atomic SQL migration runner, historical membership/leadership, KPI versions, evaluations/reviews, Jira facts, snapshots, audit/session models; code/offline verification PASS; npm audit reports 0 vulnerabilities at that checkpoint.
- T03 deterministic domain/scoring engine: safe threshold/range/formula/hybrid scoring, score/KPI bounds, first-class NOT_EVALUATED, rank validation/resolution, period-aware membership/KPI assignment resolution, lifecycle/recalculation/finalize/lock guards, adjustment reason policy, historical aggregation. Final verification: lint PASS, typecheck PASS, 46/46 tests PASS, production build PASS; code/docs review completed.
- T04 authentication/RBAC foundation: scrypt password hashing (`N=131072, r=8, p=1`), opaque DB-backed sessions with only HMAC token hashes persisted, HttpOnly/SameSite=Strict session cookies, DB-shared login throttling, inactive/expired/revoked session rejection, origin/fetch-site mutation guard, server-derived role permissions, and resource-scope helpers for team/member evaluation access. Auth routes `/api/auth/login`, `/api/auth/logout`, `/api/auth/me` build successfully. Final verification: lint PASS, typecheck PASS, 64/64 tests PASS, production build PASS; post-build security review completed.
- T05-A organization persistence checkpoint: explicit user↔organization access, tenant-scoped Organization/Team/Member/Membership/KPI Template APIs, primary-membership overlap locking, case-insensitive uniqueness, and expected conflict mapping. Verification: lint PASS, typecheck PASS, 73/73 tests PASS, production build PASS.
- T05-B KPI configuration/lifecycle checkpoint: tenant-scoped KPI Version/Criteria/Metric/Scoring APIs, transactional version cloning, deterministic formula syntax validation, exact-10 submission validation, separate `kpi:manage`/`kpi:approve` permissions, serialized submit/approve/publish/retire transitions, and PostgreSQL mutation-boundary triggers freezing submitted configuration. Final code/offline verification: lint PASS, typecheck PASS, 85/85 tests PASS, production build PASS; three review rounds fixed formula-validation, total-score error mapping, DB immutability, cascade behavior, and submit-vs-edit races.
- T05-C authenticated UI/persistence checkpoint: the application shell is session-gated, organization context comes from authenticated access, the demo role switcher is removed as authority, Teams/Members/KPI Templates use protected APIs, and `src/components/kpi-builder.tsx` provides API-backed version/criterion/metric/scoring/lifecycle configuration with submitted drafts read-only and clone-to-edit behavior. Client transport/state tests cover route payloads, structured API errors, and builder selection behavior. Final code/offline verification: lint PASS, typecheck PASS, 20 test files / 92 tests PASS, production build PASS; code/data-integrity review confirmed multi-source evidence preservation, safe template/version reselection, and preservation of non-threshold scoring rules while editing threshold rules.
- T06 authoritative evaluation pipeline: protected period/assignment/lifecycle/evaluation/result APIs, effective membership/team/KPI-version resolution, canonical `pipeline + CollectedMetricInputProvider`, deterministic ordered scoring fallback, MANUAL/NOT_EVALUATED semantics, evidence/data-quality persistence, and API-backed Evaluation Periods/System Evaluation UI. Security review separated `evaluation:run` from trusted `evaluation:ingest`. Migrations `0007`/`0008` freeze submitted metric semantics and all assignment mutation shapes once collection starts and require exact resolved membership for persisted evaluations. Live PostgreSQL verification persisted 14/14 eligible evaluations and 55 scored criteria; deliberate missing data remained CRITICAL/NOT_EVALUATED. Final gates at T06 checkpoint: lint PASS, typecheck PASS, 22 files / 106 tests PASS, production build PASS. Later T10-H real system-Chrome E2E now covers Evaluation Periods/System Evaluation click-through with zero page errors.
- T07 authoritative review/finalize/lock: API-backed Leader Review and Department Head Calibration, period-effective Team Leader and Department Head resource scope, layered criterion scores, append-only reasoned adjustments, auditable CRITICAL quality resolve/waive, final score/rank derivation, canonical SHA-256 historical snapshots, and PostgreSQL final-state immutability. `0009_review_finalization_lock.sql` freezes finalized/locked outcomes, adjustments and snapshots; `0010_review_scope_quality_resolution.sql` adds effective Department Head assignments and immutable quality-resolution actor/reason metadata. Live proof: Department Head queue 0 without assignment → 14 with effective assignment → 0 after removal; one critical-data member reached LOCKED with final score/rank and persisted snapshot; locked evaluation, snapshot, and resolved-quality rewrites were all DB-blocked. Final gates: lint PASS, typecheck PASS, 24 files / 116 tests PASS, production build PASS.
- T08 Jira integration boundary: Admin-only Jira connector/mapping/sync observability and normalized current facts; server-owned Jira metric aggregation; `/evaluate-jira` under `evaluation:run`; exact contributing-fact snapshots; snapshot-first reruns; lifecycle-safe recalculation. Live local proof evaluated 14/14 members, persisted 84 issue snapshots, deliberately mutated Jira current state, and verified both snapshot digest and evaluation scores remained unchanged on rerun. Final gates: lint PASS, typecheck PASS, 28 files / 128 tests PASS, production build PASS.
- T09 authoritative audit and historical analytics: tenant-scoped `audit_events`, PostgreSQL append-only UPDATE/DELETE guards, actor-retention policy, request-ID correlation, actor identity snapshots, same-transaction audit writes for review/finalize/lock, quality disposition, KPI lifecycle/configuration, master data, Jira configuration and evaluation-period assignment/lifecycle mutations; API-backed Audit Log and Historical Analytics; history aggregation uses only FINALIZED/LOCKED values while non-final rows affect coverage only. Live proof returned historical score 8.72 from 14 valid of 28 persisted evaluations, blocked audit UPDATE/DELETE and audited-actor hard-delete, and preserved actor reference/snapshot after deactivation. Final gates: lint PASS, typecheck PASS, 29 files / 134 tests PASS, production build PASS.
- T10-A/B/C/D hardening checkpoint: Next.js standalone runtime, non-root multi-stage Dockerfile, CI workflow, migration-parity verifier, operations/backup/restore/release runbooks, standalone runtime health/readiness proof, API-backed Executive Dashboard, Metric Library, Scoring Rules inventory, Data Quality and Rank Schemes, period rank-scheme selection, and same-transaction audit coverage for KPI metric/version/criterion/metric-binding/scoring-rule writes. Live proofs: migration parity 14/14 through `0014_user_onboarding`, production dependency audit 0 vulnerabilities, standalone `/api/health` 200 + `/api/ready` 200, Dashboard/History API-backed evidence, configuration-surface persistence, KPI config request-ID/actor audit retention, real system-Chrome mutation/reload navigation, and isolated backup/restore + restored standalone readiness PASS. Docker image build remains environment-blocked because Docker CLI is unavailable locally.
- T10-E/F administration/onboarding checkpoint: API-backed Administration Settings now lists organization users and period-effective Department Head assignment history, creates/closes Department Head scope with same-transaction audit, and provisions organization users with optional member linkage. Migration `0014_user_onboarding.sql` adds forced first-login password rotation and one-to-one non-null member↔user linkage. Temporary-password sessions may access `/api/auth/me`, logout and password rotation only; organization APIs return `PASSWORD_CHANGE_REQUIRED` until rotation succeeds. Password rotation revokes other sessions and appends non-secret `PASSWORD_CHANGED` audit events for active organization access. Repeatable live proofs: Department Head create/close scope + request-ID audit correlation PASS; onboarding provision/member-link/audit PASS; audit contains no password material; temporary org access blocked; rotation clears the flag; old temporary password rejected; new password accepted. PostgreSQL unique conflicts wrapped by Drizzle are normalized to HTTP 409, and optional member linking is transaction-serialized with a member-scoped advisory lock plus a conditional unlinked-row update. T10-E/F checkpoint gate: lint PASS, typecheck PASS, 29 files / 140 tests PASS, production build PASS.

- T10-G observability checkpoint: `/api/*` requests receive stable correlation/start metadata through `src/proxy.ts`; centralized API responses emit structured completion logs, normalized route/status/error counters, request-duration histograms and `Server-Timing`; `src/instrumentation.ts` captures uncaught framework request errors; readiness exports explicit status/outcome metrics; and `/api/metrics` exposes Prometheus-compatible process-local telemetry. Production scraping requires an independent `METRICS_TOKEN` and route labels replace UUID/numeric identifiers to bound cardinality. Live local proof on port 3712 returned health 200, readiness 200, unauthenticated probe 401, correlated request/timing headers, and request/duration/error/readiness metrics. Logger boundary now also redacts sensitive-key fields, bearer tokens and credential-bearing URLs before JSON emission, with circular/Error safety. Full gate: lint PASS, typecheck PASS, 31 files / 146 tests PASS, production build PASS.

## In progress
- T15 remote GitHub CI is DONE: initial run exposed one CI-only metrics-auth test isolation defect; fix commit c9274f8 reran successfully.
- T16 remote container image/runtime proof is now active through GitHub Actions because the local host has no Docker/Podman.
- T14 real Jira verifier harness is complete; credentialed execution is BLOCKED_EXTERNAL by missing JIRA_REAL_WORKSPACE_URL, JIRA_REAL_JQL, and JIRA_BACKEND_CREDENTIALS.
- T17–T18 remain target-production infrastructure/final-GO work.

## Known production blockers
- Active production shell/components no longer import `src/lib/kpi.ts`; Dashboard, Metric Library, Scoring Rules, Data Quality and Rank Schemes are now persisted/API-backed. `src/lib/kpi.ts` remains only as an unreferenced legacy fixture and is not production authority.
- A credentialed local PostgreSQL development environment exists and migrations `0001`–`0014` have executed successfully there. Local isolated backup/restore rehearsal now PASSes with 14/14 migration parity, representative data, integrity triggers and restored standalone readiness. Production/CI database provisioning, secret distribution, target pre-migration backup, and deployment validation remain external release gates.
- The product shell is login/session-gated and local credentialed auth/organization/evaluation APIs have been integration-tested. Administrator user provisioning is now API/UI-backed with optional member linkage, forced first-login password rotation, other-session revocation, and non-secret audit events.
- Browser click-through/reload regression coverage PASSes through Playwright against installed system Chrome; the ChatCode browser engine is not required for the release evidence path.
- Evaluation and human review/finalization/lock are authoritative for the migrated workflows, including DB-enforced final-state immutability and auditable quality disposition.
- Department Head scope is period-effective through `department_head_assignments`; Administration Settings now lists, creates and safely closes effective-dated assignments with request-correlated audit history.
- Jira T08-A/T08-B are locally end-to-end verified, including snapshot persistence and frozen-rerun behavior. Real Atlassian credentialed sync remains unverified and is still a production integration blocker.
- Critical local browser E2E is now repeatable and PASSing; remaining coverage gap is environment-specific production smoke/acceptance rather than missing local browser regression.
- Deployment, migration, backup/restore and release runbooks now exist, but production-environment deploy/restore execution has not yet been validated; Docker image build is locally environment-blocked because Docker CLI is unavailable.

## Next gate
Next release closure sequence: prove T16 container build/non-root/runtime health/readiness in GitHub Actions; execute T14 real Jira credentialed proof when approved workspace/JQL/credentials exist. T17–T18 remain target-production gates for secrets/infrastructure/telemetry/deploy/rollback/smoke and final GO. T15 remote CI, local system-Chrome E2E, aggregate release proof and isolated restore rehearsal are already PASS and must not be reopened as blockers.
